import { Loading, Wrapper } from "./LoadingComponent.styles";

function LoadingComponent() {
  return (
    <Wrapper>
      <Loading>Loading...</Loading>
    </Wrapper>
  );
}

export default LoadingComponent;
